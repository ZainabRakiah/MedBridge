{
  "name": "medbridge",
  "version": "2.0.0",
  "private": true,
  "description": "MedBridge — AI Medical Scribe & Multi-Source Medical History Bridge",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
